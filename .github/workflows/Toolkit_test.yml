name: AzureBlobStorage Toolkit Test
permissions:
  contents: read

on:
  push:
    branches:
      - '**'
    paths:
      - 'Frends.AzureBlobStorage.Toolkit/**'
  workflow_dispatch:

jobs:
  build:
    uses: FrendsPlatform/FrendsTasks/.github/workflows/toolkit_test.yml@main
    with:
      working-directory: 'Frends.AzureBlobStorage.Toolkit'
      project-directory: 'Frends.AzureBlobStorage.Toolkit'
    secrets: inherit
